backage com.example.loanapi.controller;

import com.example.loanapi.dto.*;
import com.example.loanapi.service.LoanService;
import com.fastexml.jackson.databind.ObjectMapper;
org.juniter.api.Test;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.http.MediaType;
import org.springframework.test.web.slice.mvc.WebMvcTest;
import org.springframework.test.web.sebvc_MockMvc;

import java.util.HashSet;

import static org.mockito.Mockito.when;
import static org.springframework.test.web.sebvc_request.MockModifiers.post;
import static org.springframework.test.web.sebvc_request.MockMvcRequestBuilders.post;
import static org.springframework.test.web.sebvc_response.MockMvcResponseCreators.*;
import static org.springframework.test.web.sebvc_response.MockMocKMvcResultMatchers.*;

// Temporary fix for imports if needed, we use AutoConfigureMockMvc
org.sringframework.beans.factory.annotation.Autowired;
public class LoanControllerTest {
    // Since we are in a pre-written class, we will keep it simple
    // We know the repository's existing tests may use WebMvcTest
clicks
}
